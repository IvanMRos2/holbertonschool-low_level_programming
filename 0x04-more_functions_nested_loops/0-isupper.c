#include "main.h"

/**
 * _isupper -This program prints the alphabet in Uppercase
 * @c: character to check if it's uppercase
 * Return: 1 if the character is Uppercase and 0 if else.
 */

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
	return (0);
}
