#include <stdio.h>

/**
 * main - This program prints the alphabet in lowercase
 *
 * Return: zero
 */

int main(void)
{
	char alf;

	for (alf = "a"; alf <= "z\n"; alf++)

		putchar(alf);

	return (0);
}
