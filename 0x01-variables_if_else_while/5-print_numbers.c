#include <stdio.h>

/**
 * main - This program prints the numbers
 * only if they are less than 10
 * Return: zero
 */

int main(void)
{
	int ten;

	for (ten = 0; ten < 10; ten++)
	{	printf("%d", ten);
	}
	printf("\n");
	return (0);
}
