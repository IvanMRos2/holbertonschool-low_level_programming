#include "main.h"

/**
 * add - It makes the addition of two numbers
 * @p: It's one number
 * @q: It's one number
 * Return: prints r
 */

int add(int p, int q)
{
	int r;

	r = p + q;
	return (r);
}
