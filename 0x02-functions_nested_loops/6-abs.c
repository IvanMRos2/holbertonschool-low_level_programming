#include "main.h"

/**
 * _abs -This project prints the absolute value of integer numbers
 * @I: The variable for the integer number
 * Return: The absolute value of the integer number
 */

int _abs(int I)
{
	if (I >= 0)
	{
		return (I);
	}
	else
		return (-I);
}

