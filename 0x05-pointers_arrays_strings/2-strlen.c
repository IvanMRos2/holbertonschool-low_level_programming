#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 * _strlen - This function returns the length of a string
 * @s: The variable that points the string
 * Return: L
 */

int _strlen(char *s)
{
	int L;

	L = strlen(s);
	return (L);
}
